package example.inline_properties_abstracts;

/**
 * ...
 * @author Skial Bainn
 */
class Main {

	public static function main() {
		trace( MyStatic.some );
		trace( 'some plus = ' + MyStatic.some );
		
		trace( MyStatic.thing );
		trace( 'thing plus = ' + MyStatic.thing );
	}
	
}