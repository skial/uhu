package uhx.macro.jumla.a;

import haxe.macro.Type;
import haxe.macro.Expr;
import haxe.macro.Context;

/**
 * ...
 * @author Skial Bainn
 */
abstract Meta(Metadata) from Metadata to Metadata {

	public function new(v:Metadata) this = v;
	
}